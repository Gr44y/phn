# phn
phn is a PHP CLI program that displays the latest in Linux from phoronix 



# To install phn chmod the install.sh then run the install.sh script in the phn directory
### To uninstall: sudo rm /usr/local/bin/phn

## To limit or unlimit the amount phn shows add a number after phn, 1-50 example: phn 43
